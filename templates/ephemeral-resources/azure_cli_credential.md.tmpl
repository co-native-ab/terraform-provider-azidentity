---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}


## Example Usage

{{ tffile (printf "examples/ephemeral-resources/%s/ephemeral-resource.tf" .Name)}}

## Grafana Provider Example

This is an example to show how to use the `{{.Name}}` ephemeral resource with the [grafana terraform provider](https://registry.terraform.io/providers/grafana/grafana/latest/docs) and [Azure Managed Grafana](https://learn.microsoft.com/en-us/azure/managed-grafana).

{{ tffile (printf "examples/ephemeral-resources/%s/grafana-provider-example.tf" .Name)}}

*NOTE: Even though we are using the `{{.Name}}` ephemeral resource, it should work with any of the other credentials available in this provider.*

{{ .SchemaMarkdown | trimspace }}
{{- if .HasImport }}

## Import

Import is supported using the following syntax:

{{ printf "{{codefile \"shell\" %q}}" .ImportFile }}
{{- end }}