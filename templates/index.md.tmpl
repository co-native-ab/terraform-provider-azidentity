---
page_title: "Provider: azidentity"
description: |-
  The azidentity provider is used to interact with the Azure Identity service.
---

# azidentity Provider

The azidentity provider is used to interact with the Azure Identity service. The provider itself has no configuration options.

Use the navigation to the left to read about the available ephemeral resources.
